# Copier ce fichier en `.env` et adapter les valeurs. Ne jamais commiter `.env`.
# Variables serveur uniquement (jamais envoyées au navigateur).

OIDC_ISSUER=http://localhost:8080/realms/paios
OIDC_CLIENT_ID=paios-api
API_URL=http://localhost:8000
APP_URL=http://localhost:3000
